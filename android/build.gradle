ext {
    androidxAppCompatVersion = project.hasProperty('androidxAppCompatVersion') ? rootProject.ext.androidxAppCompatVersion : '1.2.0'
}

buildscript {
    repositories {
        google()
        jcenter()
        mavenLocal()
        mavenCentral artifactUrls:["/home/hugo/.m2/repository"]

        maven {
            url = uri("https://maven.springframework.org/release")
        }

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.2.1'
    }
}

apply plugin: 'com.android.library'
apply plugin: 'com.google.cloud.artifactregistry.gradle-plugin'

android {
    compileSdkVersion project.hasProperty('compileSdkVersion') ? rootProject.ext.compileSdkVersion : 30
    defaultConfig {
        minSdkVersion project.hasProperty('minSdkVersion') ? rootProject.ext.minSdkVersion : 21
        targetSdkVersion project.hasProperty('targetSdkVersion') ? rootProject.ext.targetSdkVersion : 30
        versionCode 1
        versionName "1.0"

    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    lintOptions {
        abortOnError false
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}
allprojects {
    repositories {
        google()
        jcenter()
        maven {
            url "artifactregistry://us-central1-maven.pkg.dev/jaakrecog-1to1-similarity/jaakrecog-maven"
        }

        mavenrepository {
            url = uri("https://maven.springframework.org/release")
        }
    }
}




dependencies {

    implementation project(':capacitor-android')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation 'com.google.android.material:material:1.4.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.0'
    implementation 'jaakrecog.jaakit.fingerprint:finger-capture:1.0.0'
    implementation 'jaakrecog.jaakit.fingerprint:finger-analysis:1.0.0'
    compile group: 'com.jaakit.fingeracequisition', name: 'finger-capture', version: '1.0.0'

}
